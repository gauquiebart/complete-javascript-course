'use strict';

/*console.log('===========================')
console.log('===========================')
console.log('===========================')
console.log('===========================')
console.log('===========================')
const worldPopulation = 7900000000;

function percentageOfWorld1(population) {
    return population / worldPopulation * 100;
}

console.log(`china pct ${percentageOfWorld1(1441000000)}`);

const percentageOfWorld = function(population) {
    return population / worldPopulation * 100;
}

console.log(`china pct ${percentageOfWorld(1441000000)}`);

*/
/*
const myCountry = {
    country: 'Belgium',
    capital: 'Brussels',
    language: 'Nederlands',
    population: 11000000,
    neighbours: ['Germany', 'France', 'Luxembourg'],
    describe: function() {
        return `${this.country} has ${this.population} million ${this.language} speaking people, ${this.neighbours.length} neighbouring countries and a capital called ${this.capital}` 
    },
    checkIsIsland: function() {
        this.isIsland = this.neighbours.length > 0;
        return this.isIsland;
    }
}

console.log(myCountry);

console.log(`${myCountry.country} has ${myCountry.population} million ${myCountry.language} speaking people, ${myCountry.neighbours.length} neighbouring countries and a capital called ${myCountry.capital}`);

myCountry.population = myCountry.population + 2000000;
myCountry['population'] = myCountry['population'] + 2000000;

console.log(myCountry);
console.log(myCountry.describe());

console.log(myCountry.checkIsIsland());
console.log(myCountry.isIsland);
*/
/*
//coding challenge two

const markMiller = {
    name: 'Mark Miller',
    mass: 80,
    height: 1.80,
    calcBmi: function() {
        this.bmi = this.mass / this.height ** 2; 
        return this.bmi;
    }
};

const johnSmith = {
    name: 'John Smith',
    mass: 60,
    height: 1.60,
    calcBmi: function() {
        this.bmi = this.mass / this.height ** 2;
        return this.bmi;
    }
}

console.log(markMiller, johnSmith);
console.log(markMiller.calcBmi(), johnSmith.calcBmi());
console.log(markMiller.bmi, johnSmith.bmi);


*/


